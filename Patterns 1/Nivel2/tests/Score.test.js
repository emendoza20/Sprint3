const Score = require("../src/Score");

describe("Given a Only instance of a score", () => {
  describe("Show the score", () => {
    test("then should throw an error ", () => {


        const score1 = new Score()
        score1.

        //expect(new Score()).toBeInstanceOf(Score);
        //expect(() => {}).toBeInstanceOf(Function);
        //expect(new Score()).toBeInstanceOf(Function); 

    });
  });
});
